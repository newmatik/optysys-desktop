{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: main.py",
      "type": "python",
      "request": "launch",
      "program": "main.py",
      "python": "${workspaceFolder}/venv/bin/python" // only used for linux and macos
      // "python": "${workspaceFolder}/venv/Scripts/python.exe" // only used for windows
    }
  ]
}
